// შექმენით სია და გადაუარეთ მას while && do while-ს გამოყენებით.

let list = ["random", "rame", "ari ra", "eyofa otxi"]

let i = 0
while(i < list.length){
    console.log(list[i])
    i++
}

let b = 0

do {
    console.log(list[b])
    b++
} while(b < list.length)