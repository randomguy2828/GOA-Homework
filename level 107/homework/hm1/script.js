// შექმენით ფუნქცია რომელიც არგუმენტად გადაცემულ რიცხვზე დაგვიბრუნებს რიცხვი ლუწია თუ კენტი

let check1 = (num) => {
    return num % 2 === 0 ? "ლუწია" : "კენტია"
}

let check2 = function(num) {
    return num % 2 === 0 ? "ლუწია" : "კენტია"
}
