// 6)მოცემულია მასივი:

// const colors = ['red', 'green', 'blue', 'yellow'];

// გამოიტანე ყველა ფერი for ციკლის გამოყენებით

const colors = ['red', 'green', 'blue', 'yellow'];

for(let i = 0;i < 4;i++){
    console.log(colors[i])
}